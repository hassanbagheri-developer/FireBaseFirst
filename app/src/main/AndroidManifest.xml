<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireBaseFirst">
        <activity android:name=".Activiy.AddNoteActivity"></activity>
        <activity android:name=".Activiy.Phone.VerifyPhoeAuth" />
        <activity android:name=".Activiy.Phone.PhoneAuthActivity" />
        <activity android:name=".Activiy.ForgetPasswordActivity" />
        <activity android:name=".Activiy.MainActivity" />
        <activity android:name=".Activiy.SignUpActivity" />
        <activity android:name=".Activiy.MainSignUpActivity" /> <!-- <activity android:name=".LoginActivity"></activity> -->
        <activity android:name=".Activiy.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>